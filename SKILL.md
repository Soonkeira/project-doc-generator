---
name: project-doc-generator
description: Use when the user asks to “形成项目文档” or “生成项目文档”, requests a requirements/design/API/test/deployment document set from a codebase, or explicitly invokes $project-doc-generator; also use when the user asks business questions about a project — such as “这个功能是怎么实现的”“为什么这里不能删除”“订单取消以后做了什么” — and expects the answer to be located through the project knowledge map and business logic entries and traced back to source code evidence.
---

# 项目文档生成器

> 版本：v1.4.1
> 作者：[Soonkeira](https://github.com/Soonkeira)

根据项目代码生成可追溯、可校验的项目知识层与传统文档套件；不把推断内容伪装成已验证事实。

## 核心定位

```
Source Code → 代码与业务分析 → Project Knowledge Layer → 传统 7 套项目文档
```

- **知识层是核心**：它回答“系统是怎么工作的？”，把业务能力、业务规则、入口/API、调用链、数据变化、外部依赖、源码文件/符号和测试串成一条可反向定位的证据链。
- **传统 7 套文档是知识层的展示视图**：它们回答“项目按照软件工程标准应该如何描述？”，内容以知识层为事实来源。
- 知识层包含：项目知识地图、业务逻辑条目（BL）、源码反向索引（Source Map，即 `00-源码索引.md` / `00-Source Index.md`）、调用链、数据影响、API/DB/MQ/外部服务关系、Git 验证状态。
- **双向索引**：业务逻辑条目 → 源码（各 BL 的「源码证据」「数据变化」「触发入口」「外部影响」「相关测试」）与源码 → 业务逻辑条目（`00-源码索引.md`）是同一套索引的两半，缺一不可；两边的路径、符号、表、入口、配置项、外部依赖/资源、测试必须一致，不一致视为校验失败。
- 传统 7 套文档必须完整保留（兼容），不删除、不重命名。

## 调用方式与默认值

- 直接调用：`$project-doc-generator 生成当前项目的完整项目文档`
- 自然语言触发：`形成项目文档` 或 `生成项目文档`
- 查询触发：用户提出“这个功能是怎么实现的？”“订单取消以后做了什么？”这类业务问题时进入查询模式。
- 同步触发：用户要求“同步项目文档”“同步知识层”“更新项目知识层”这类增量维护时进入同步模式。
- 默认分析当前工作区；用户提供项目路径时以该路径为准。
- **运行模式**（三种）：
  - **生成模式**：全量扫描 → 建立知识层（知识地图 + 源码索引 + BL 条目）+ 7 套传统文档。**会写入文件。**
  - **查询模式**：**默认只读**。这里的「只读」指**不修改任何文件**，**不是不读源码**；查询模式**不写任何文件**。
    - **正常路径**：问题 → 读 `Doc/<项目名称>/cn/00-项目知识地图.md`（英文 `Doc/<项目名称>/en/00-Project Knowledge Map.md`）定位相关 BL 条目 → 读取该 BL 条目 → 用其中的调用链、业务规则、数据变化和源码证据直接作答。
    - **即时核实路径**：当 BL 条目缺失、时效状态不是 `最新`（含 `⚠ 可能过期`）、或证据不足以回答时，不得只回答“文档可能过期”就结束，必须继续核实：BL → `00-源码索引.md`（英文 `00-Source Index.md`）定位符号与文件 → **读取实际源码** → 只读核实（必要时读 Git diff 与相关测试）→ 作答。
    - 回答时说明**核实依据**（读了哪些文件与符号、看了哪些 diff 或测试）以及结论的不确定性；**不得因为文档过期就要求用户先去跑同步模式**。
    - 允许读取源码、Git diff 与相关测试做即时核实，但**不得修改知识层或源码**；只有用户明确要求更新文档时，才转入同步模式。
    - **未经用户明确要求，不得改写任何文档**（包括 BL 条目、知识地图、源码索引和 7 套传统文档）。
  - **同步模式**：增量维护。执行变更影响分析 → 只重新验证受影响条目 → 刷新时效状态 → 更新源码索引 → 推进验证基线。**只写知识层文件**（知识地图、源码索引、BL 条目），**不改 7 套传统文档**（除非用户明确要求）。执行步骤见第 10 节；**开始前必须先做第 10 节的「运行前置判定」**（无 Git / 正式基线未建立 / 已有正式基线，三种状态互斥）。
- 默认输出：`00-项目知识地图` + `00-源码索引` + `business/BL-*.md` + 全部 7 份传统文档，输出到 `Doc/<项目名称>/cn/` 和 `Doc/<项目名称>/en/`。
- 用户可以指定语言（中文/英文/双语）、文档编号（如 `01,05`）、输出目录、作者和是否覆盖。
- 用户也可以只生成知识层，或只生成传统文档，或指定编号子集。
- 未提供其他作者时，默认作者为 [Soonkeira](https://github.com/Soonkeira)。
- 未明确指定覆盖时，若目标文档已存在，先列出冲突文件并请求确认，不直接覆盖。

## 分析边界与安全规则

- 默认跳过 `.git/`、`node_modules/`、`vendor/`、`dist/`、`build/`、`coverage/`、`.venv/`、缓存和生成物目录。
- 不读取或写入密钥、令牌和证书内容；跳过 `.env*`、`*.pem`、`*.key`、`credentials*`、`secrets*` 等敏感文件。文档中只描述配置项名称，不输出值。
- 只修改用户指定的文档输出目录，不修改源代码、配置、依赖锁文件或 Git 历史。
- 代码库过大时先说明分析范围，并优先读取入口、配置、路由、领域模型、数据库迁移、测试和部署文件。
- 对没有发现的数据库、API、测试或部署信息，写明“未发现/不适用”，禁止编造表、接口、用例或运行参数。
- **禁止**：删除 7 套传统文档、重命名 7 套传统文档、引入运行时依赖、添加数据库/Web UI/RAG/向量库、编造业务事实、把推断包装成事实、在时效判定里编造 commit 哈希。

## 源码可追溯性强化

任何重要业务结论都必须能反向定位到源码证据。

- ❌ 不能只写：“系统会检查用户权限。”
- ✅ 应写成：“系统在 `PermissionService.checkPermission()` 中检查当前用户是否具有该资源的操作权限。”
- 每条重要结论都要记录来源文件、符号/接口/表名和证据状态：`已验证`、`部分验证`、`推断`、`未发现`。
- 只能靠结构推断时，必须显式标注 `状态：推断`。**禁止把模型推断包装成事实。**
- 模板中的示例必须标注为“示例（占位，非项目事实）”。

## 工作流程

### 1. 理解项目代码
- 分析项目目录结构
- 读取核心代码文件
- 理解领域模型、数据仓储、业务逻辑
- 识别设计模式和架构
- 记录关键结论的来源文件、符号/接口/表名和证据状态：`已验证`、`部分验证`、`推断` 或 `未发现`。

代码结构分析是**手段**，业务知识提取才是最终目标。

### 2. 业务知识提取（业务问题优先）
结构分析完成后不要停手，**不要按 Controllers → Services → Models → Repositories 的顺序机械地生成文档**，必须进一步反推出业务知识：

- 系统有哪些**业务能力**？
- 每个业务能力**从哪里触发**（HTTP/API 入口、CLI、定时任务、消息消费、事件回调）？
- **业务规则**写在哪里（校验、状态机、条件分支、配置开关）？
- **调用链**是什么（入口 → 应用服务 → 领域对象 → 仓储 → 外部依赖）？
- 执行完**影响什么数据**（表、字段、缓存、消息、文件）？
- **异常与失败路径**是什么（抛错、回滚、补偿、重试、静默失败）？
- 这些结论**对应哪些源码文件与符号**？

#### BL 粒度判定

「以业务问题为单位，不以代码文件为单位」只是方向，约束不足；粒度判定按以下规则执行，目的是**保证不同模型、不同时间运行生成的知识体系规模稳定**（避免一次生成 28 条、下次生成 64 条这种漂移）：

- **BL = 用户/业务可感知的业务能力或用例（Use Case）**，不是 Controller 方法、不是技术函数。
- 多个 API 如果只是同一个业务能力的**不同入口** → 可以属于**同一个 BL**。
- 同一个 API 如果存在**完全不同的业务结果、事务边界或业务生命周期** → 可以**拆成多个 BL**。
- **CRUD 不默认「一接口一个 BL」**。
- 技术工具函数、Repository CRUD、DTO 转换**不独立成为 BL**。
- BL 以**稳定业务语义**命名（如「创建订单」「取消订单」「审核退款」「分配公网 IP」），不以技术动作命名。
- **重构 Controller / Service 层次不应导致 BL 编号改变**（ID 跟着业务语义走，不跟代码结构走）。

以业务问题为单位组织结论，而不是以代码文件为单位。提取结果写入知识层（见第 6 节）。

### 3. 版本信息获取
- 检查项目是否处于 Git 工作树，且是否存在 `HEAD`。
- 有有效 `HEAD` 时提取 Git 分支、提交次数和最近相关提交；没有有效 `HEAD` 时使用时间戳方案。
- 根据当前日期生成版本号：
  - **有 Git**：版本号格式为 `1.{yy}.{Mdd}.{git提交次数}`
    - `{yy}`：年份后两位（如 2026 年为 26）
    - `{Mdd}`：月份和日期（如 5月9日 为 509）
    - `{git提交次数}`：通过 `git rev-list --count HEAD` 获取
    - 示例：`1.26.509.1234`（2026年5月9日，第1234次提交）
  - **无 Git**：版本号格式为 `1.{yy}.{Mdd}.{hmm}`
    - `{hmm}`：小时和分钟（如 14:30 为 1430）
    - 示例：`1.26.509.1430`（2026年5月9日 14:30）

### 4. 作者信息获取
生成文档时，作者字段按以下四级优先级获取：
- **第1优先级 — 用户明确提供的作者**
- **第2优先级 — 本地默认作者**：`Soonkeira`（[GitHub](https://github.com/Soonkeira)）
- **第3优先级 — Git 提交作者**：从目标项目的最近一次提交中提取作者名（`git log -1 --format=%an`）
- **第4优先级 — 置空**：无法确认时留空（`作者：` / `Author: `）

**严禁把机器账户名（`$env:USERNAME` / `whoami`）当作作者。**

### 5. 创建文档目录
在项目根目录下创建 `Doc/<项目名称>/cn/`（中文）和 `Doc/<项目名称>/en/`（英文）子目录，以及两者的 `business/` 子目录。若已存在，先读取现有文档的结构、版本、变更日志、`00-源码索引.md`，以及知识地图「验证基线」中的 `last_verified_commit`；不得无确认覆盖。

### 6. 生成项目知识层

按 [references/cn/business-logic-template.md](references/cn/business-logic-template.md)（英文 [references/en/business-logic-template.md](references/en/business-logic-template.md)）生成：

| 中文（`Doc/<项目名称>/cn/`） | 英文（`Doc/<项目名称>/en/`） |
|------|------|
| `00-项目知识地图.md` | `00-Project Knowledge Map.md` |
| `00-源码索引.md` | `00-Source Index.md` |
| `business/BL-<NNN>-<业务名称>.md` | `business/BL-<NNN>-<business-name>.md` |

#### 项目知识地图
- **禁止只写项目简介**，必须以业务能力索引为主体：按业务域分组，表格列固定为 `| ID | 业务能力 | 描述 | 证据状态 | 时效状态 |`，`ID` 列链接到对应 BL 文件。
- 业务域分组标题只写业务域名称，**不得附带任何编号范围**（如 `（BL-xxx–BL-yyy）`）；分组只影响展示顺序，与 ID 无关。
- 必须包含「验证基线」（字段 `last_verified_commit` + 验证日期 + `工作区状态` + `验证状态` + `正式基线` + `traditional_docs_status` + `traditional_docs_generated_from_commit` + `traditional_docs_working_tree` + 「最近一次增量分析」记录）、紧随其后的「编号登记（ID Registry）」和「常见业务问题索引」。
- **`00-项目知识地图.md` = 文档状态中心**：传统文档的新鲜度三字段（`traditional_docs_status` / `traditional_docs_generated_from_commit` / `traditional_docs_working_tree`）统一记录在本文件的「验证基线」里，01–07 文档自身不增加任何 stale / current 字段（见第 10 节「传统文档新鲜度」）。
- 证据状态与时效状态**分两列独立显示**，任何地方都不得把两者合并成一条（禁止在证据状态前拼接 `⚠ 可能过期`）。

#### BL 编号规则
`BL-` + 三位数字，按**首次建立顺序递增**：`BL-001`、`BL-002`、`BL-003`…

- **取号规则**：新条目编号 = **历史最大 BL 编号 + 1**（例如当前最大是 `BL-006`，下一个新条目就是 `BL-007`）。
- 编号**永不复用、永不重排**；删除的编号**永久 tombstone，永不复用**。
- **允许因删除产生跳号**：跳号是正常的，不是错误。
- 原则：**稳定 ID 比编号连续重要得多。**
- **编号与业务域无关**：不再为业务域预留任何连续编号区间，业务域只是 metadata，不参与编号。
- 同一业务逻辑在中英文下编号一致。
- **ID 跟着业务语义走**：重构 Controller / Service 层次、移动文件、改类名都不改 ID（见第 2 节「BL 粒度判定」）。
- 为保证「永不复用」，必须在知识地图维护「编号登记（ID Registry）」；**没有编号登记就无法保证「永不复用」**（最高号条目被删除时，只看当前最大号会退回去复用已废弃的编号），取号前必须先查编号登记。

#### 编号登记（ID Registry）
知识地图中紧随「验证基线」之后的固定章节，用于支撑「永不复用」：

| 项目 | 值 |
|------|-----|
| 当前最大编号 | `BL-006` |
| 下一个可用编号 | `BL-007` |
| 已废弃编号（tombstone，永不复用） | `BL-003`（原「取消订单」，YYYY-MM-DD 废弃） |

- 新条目编号 = **历史最大编号 + 1**（`历史最大编号` 取「当前最大编号」与「已废弃编号」中的最大值，因此删除最高号条目后取号不会退回复用）。
- 已废弃编号**永久保留占位、不得复用**；跳号正常，不做补号、不做重排。
- 条目删除、合并或废弃时，必须同步更新本章节：把编号移入「已废弃编号」并记录原业务名称与废弃日期。

#### 业务域（metadata）
- 业务域写在 BL 条目头部 metadata 块的 `**业务域**：<业务域名称>` 字段里。
- 业务域重新分类时**只改这个字段，绝不改 ID**。
- 知识地图仍按业务域分组展示（便于阅读），但分组与 ID 分配互相独立。

#### 两个互相独立的字段
- **证据状态**（模型判断，四选一）：`已验证` / `部分验证` / `推断` / `未发现`。
- **时效状态**（不是模型主观判断，四选一）：`最新` / `⚠ 可能过期` / `临时工作区分析` / `未发现（无 Git 基线）`。**时效状态以 Git 变更检测为基础，并结合 Source Map 与多层影响分析判定。**
- 两者互相独立，**严禁混用**，也**严禁合并显示**；判定规则与术语分工见第 10 节「时效状态判定（四值）」。

#### BL 条目固定章节
每个 BL 条目必须包含以下 15 个章节，顺序与标题逐字一致：

1. 业务说明
2. 触发入口
3. 前置条件
4. 业务规则
5. 核心执行流程
6. 关键分支
7. 数据变化
8. 外部影响
9. 异常与失败路径
10. 源码证据
11. 相关测试
12. 关联业务
13. 证据状态
14. 时效状态
15. 最后验证版本

BL 条目头部 metadata 块（项目名称 / 业务域 / 作者 / 日期 / 版本 + 变更日志）不计入这 15 个章节。

「最后验证版本」记录该条基线（Git commit + 验证日期）；工作区 Dirty（见第 10 节）时**保留正式基线 commit**，并追加一行说明「本次分析基于未提交工作区，未推进正式基线」。「源码证据」列出可反向定位的文件、符号、表；「时效状态」按第 10 节「时效状态判定（四值）」填写 —— **以 Git 变更检测为基础，并结合 Source Map 与多层影响分析判定**，不得由模型主观填写。

#### 源码索引（Source → BL 反向索引）
`00-源码索引.md`（英文 `00-Source Index.md`）是 **Source → BL 反向索引**：原来的 BL → Source 是单向的，本文件补上从源码反查业务逻辑的另一半。

- **由各 BL 条目的「源码证据」「数据变化」「触发入口」「外部影响」「相关测试」汇总生成，必须与 BL 条目一致；不一致视为校验失败。**
- 含**六张**反向索引表：
  - 第 1 张 源码文件与符号 → BL：`| 源码路径 | 符号 | 类型 | 覆盖的 BL |`（数据来源：各 BL 的「源码证据」）
  - 第 2 张 数据表 / 迁移 → BL：`| 表名 | 变更来源 | 覆盖的 BL |`（数据来源：各 BL 的「数据变化」）
  - 第 3 张 API / 入口 → BL：`| 入口标识 | 类型 | 覆盖的 BL |`（数据来源：各 BL 的「触发入口」）
  - 第 4 张 配置项 → BL：`| 配置项 | 覆盖的 BL |`（数据来源：各 BL 的「外部影响」「前置条件」）
  - 第 5 张 外部依赖 / 资源 → BL：`| 类型 | 标识 | 覆盖的 BL |`（数据来源：各 BL 的「外部影响」）
    - 类型含 Kafka Topic / MQ / Redis Key / Cache / External API / Object Storage / File System / SMS / Email 等。
    - 示例：`| Kafka Topic | order.cancelled | BL-023 |`
  - 第 6 张 测试 → BL：`| 测试文件 | 测试方法 | 覆盖的 BL |`（数据来源：各 BL 的「相关测试」）
- 另含：未覆盖的源码文件（扫描到但无任何 BL 引用的重要文件）、覆盖范围与限制、维护规则。
- 带标准文档头部（项目名称 / 作者 / 日期 / 版本 + 变更日志），格式见第 9 节。

### 7. 生成传统文档套件

按用户选择生成以下文档；未指定时生成全部 7 份。这些文档以知识层为事实来源。

| 序号 | 中文文档 (`Doc/<项目名称>/cn/`) | 英文文档 (`Doc/<项目名称>/en/`) | 内容要点 |
|------|---------------------|---------------------|----------|
| 01 | `01-需求规格.md` | `01-Requirement Specification.md` | 功能概述、功能需求清单、业务规则需求、非功能需求 |
| 02 | `02-概要设计.md` | `02-Overview Design.md` | 系统架构、模块划分、类图关系、核心流程概要、依赖关系 |
| 03 | `03-详细设计.md` | `03-Detailed Design.md` | 每个方法的算法流程、分支逻辑、关键实现细节 |
| 04 | `04-数据库设计.md` | `04-Database Design.md` | 数据表结构、字段定义、表间关系、索引建议 |
| 05 | `05-API文档.md` | `05-API Documentation.md` | 接口清单、入参出参、调用示例、异常场景 |
| 06 | `06-测试计划.md` | `06-Test Plan.md` | 单元测试用例、边界条件、并发测试、测试数据准备 |
| 07 | `07-部署手册与用户手册.md` | `07-Deployment & User Manual.md` | 环境要求、配置项、部署步骤、操作指南、常见问题 |

每份文档的重要结论都应附带简短的“来源与证据”说明；对推断内容标注推断依据，对未发现内容标注限制，不使用模板示例数据冒充项目事实。

### 8. 版本管理

#### 版本号规则
- **有 Git 版本控制**：版本号格式为 `1.{yy}.{Mdd}.{git提交次数}`
  - `{yy}`：年份后两位（如 2026 年为 26）
  - `{Mdd}`：月份和日期（如 5月9日 为 509）
  - `{git提交次数}`：通过 `git rev-list --count HEAD` 获取
  - 示例：`1.26.509.1234`（2026年5月9日，第1234次提交）
- **无 Git 版本控制**：版本号格式为 `1.{yy}.{Mdd}.{hmm}`
  - `{hmm}`：小时和分钟（如 14:30 为 1430）
  - 示例：`1.26.509.1430`（2026年5月9日 14:30）
- Git 仓库没有提交时使用无 Git 方案；不要把失败的 `git` 命令输出写入文档。

#### Git 提交记录提取
- 使用 `git rev-list --count HEAD` 获取总提交次数
- 使用 `git log --pretty=format:"%h - %s (%an, %ar)" --date=short` 获取最近提交记录
- 提交记录用于生成变更日志和版本历史

#### 变更日志格式
在每个文档头部包含变更日志。首次生成时写“初始版本”；已有文档时保留历史记录并追加本次版本，不重复覆盖历史：

```markdown
## 变更日志

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| 1.26.509.1234 | 2026-05-09 | <动态获取> | 初始版本 |
| 1.26.510.1250 | 2026-05-10 | <动态获取> | 新增XXX章节，修正XXX描述 |
```

### 9. 文档头部信息

每个文档必须包含：

```markdown
# 文档标题

**项目名称**：XXX项目
**作者**：<动态获取>
**日期**：2026-05-09
**版本**：1.26.509.1234

## 变更日志
...
```

> **说明**：作者字段通过第 4 节「作者信息获取」中定义的四级优先级动态获取，不再使用硬编码的固定作者名，也不使用机器账户名。

### 10. 增量维护与时效判定

#### 运行前置判定（三种状态，互斥）

增量维护开始前先判定项目处于下列哪一个分支；**三个分支互斥、不能混在一起**，判定结果决定本轮走增量还是全量：

- **分支 A — 无 Git（或 HEAD 无效）**：使用「无 Git」降级模式；时效状态记 `未发现（无 Git 基线）`；每次运行需**全量复核**；不得编造 commit 哈希。
- **分支 B — 有 Git，但正式基线尚未建立**（`last_verified_commit` 为空 / `正式基线` = `未建立`）：
  - **不执行任何 `<base>..HEAD` 形式的增量 diff**（此时没有合法的 `<base>`）；
  - 执行**全量复核**；
  - 若工作区 **Clean**：全量复核完成后建立正式基线 —— `last_verified_commit` = 当前 HEAD、`正式基线` = 当前 HEAD、`验证状态` = `正式基线`、`工作区状态` = `Clean`、本次验证的条目 = `最新`；
  - 若工作区 **Dirty**：继续保持 `验证状态` = `临时工作区分析`、`工作区状态` = `Dirty`；`last_verified_commit` 与 `正式基线` 保持 `未建立`；**不建立正式基线、不标记 `最新`**（本次分析的条目记 `临时工作区分析`）。
- **分支 C — 已有正式基线**：执行正常增量同步（变更文件集 = 提交部分 `git diff --name-status -M <base>..HEAD` ∪ 工作区部分 `git status --porcelain`，再做多层影响分析）。

**严禁**把「有 Git 但正式基线未建立」（分支 B）写成 `未发现（无 Git 基线）`：Git 实际存在，这两件事必须区分 —— 分支 A 是**没有 Git（或 HEAD 无效）**，分支 B 是**有 Git、有 HEAD，但还没有一个合法的正式基线 commit**，因此不能做增量 diff，只能全量复核。

分支 B 的知识地图「验证基线」必须支持下面这组取值组合（**正式基线未建立 + 工作区 Dirty** 是合法状态，不是错误）：

| 字段 | 取值 |
|------|------|
| `last_verified_commit` | `未建立` |
| `正式基线` | `未建立` |
| `验证状态` | `临时工作区分析` |
| `工作区状态` | `Dirty` |

#### 首次运行（生成模式）
1. 全量扫描项目代码。
2. 建立 `00-项目知识地图.md`（含「验证基线」与紧随其后的「编号登记（ID Registry）」）。
3. 生成全部 `business/BL-*.md` 条目，取号按「编号登记」执行（历史最大编号 + 1，永不复用已废弃编号）。
4. 汇总各 BL 条目生成 `00-源码索引.md`（六张反向索引）。
5. 执行 `git status --porcelain` 判定**工作区状态**；首次运行时正式基线尚未建立，按**分支 B**处理（见「运行前置判定」）：
   - **Clean**：用 `git rev-parse HEAD` 取当前 commit，写入「验证基线」的 `last_verified_commit`（`正式基线` 同值）和验证日期，`工作区状态` 记 `Clean`、`验证状态` 记 `正式基线`，本次验证过的条目标记 `最新`（即全量复核完成后建立正式基线）。
   - **Dirty**：**不得推进 `last_verified_commit`**（保持 `未建立`）、**不得标记 `最新`**；`工作区状态` 记 `Dirty`、`验证状态` 记 `临时工作区分析`，本次分析的条目时效状态记 `临时工作区分析`；`last_verified_commit` 与 `正式基线` 保持 `未建立`（**不得写成 `未发现（无 Git 基线）`**，Git 是存在的）。
6. 更新「编号登记」的当前最大编号、下一个可用编号与已废弃编号；写入传统文档新鲜度三字段：`traditional_docs_generated_from_commit` = 本次生成时的 HEAD commit，`traditional_docs_working_tree` 与 `traditional_docs_status` 按工作区状态取值 —— **Clean → `Clean` + `current`**；**Dirty → `Dirty` + `provisional`**（本次生成 7 套传统文档，但内容可能包含未提交代码）。

#### 后续运行（同步模式）
1. 先读已有的 `00-项目知识地图.md` 与其中的 `last_verified_commit`、`工作区状态`、`验证状态`、`正式基线`、`traditional_docs_*` 及「编号登记」，再读 `00-源码索引.md` 和全部 BL 条目。
2. 按「运行前置判定（三种状态，互斥）」判定本轮属于**分支 A / B / C** 的哪一个；**分支 A 与分支 B 一律全量复核，只有分支 C 做增量同步**：
   - **分支 A**（无 Git 或 HEAD 无效）：不做时效判定、不推进基线，全部条目录 `未发现（无 Git 基线）`，执行**全量复核**，随后跳到步骤 8（分支 A 无需执行步骤 4 的 Git 命令）。
   - **分支 B**（有 Git 但 `last_verified_commit` 为空 / `正式基线` = `未建立`）：**不执行任何 `<base>..HEAD` 形式的增量 diff**（此时没有合法的 `<base>`），执行**全量复核**（步骤 4 的 `git status --porcelain` 与 `git rev-parse HEAD` 仍须执行，用于判定工作区状态与当前 HEAD），然后按工作区状态收尾 —— Clean 则建立正式基线（`last_verified_commit` = `正式基线` = 当前 HEAD、`验证状态` = `正式基线`、`工作区状态` = `Clean`、本次验证的条目 = `最新`）；Dirty 则继续保持 `验证状态` = `临时工作区分析`、`工作区状态` = `Dirty`，`last_verified_commit` 与 `正式基线` 保持 `未建立`，**不建立正式基线、不标记 `最新`**（本次分析的条目记 `临时工作区分析`）；随后跳到步骤 8（分支 B 不执行步骤 3、5、6、7，步骤 9 按上面的 Clean / Dirty 结论执行）。
   - **分支 C**（已有正式基线）：继续执行步骤 3–11 的正常增量同步。
3. 执行**变更检测**（见下），拿到完整变更文件集。
4. 执行 `git status --porcelain` 判定**工作区状态**；执行 `git log --oneline <last_verified_commit>..HEAD` 了解提交意图；执行 `git rev-parse HEAD` 拿到当前 HEAD。
5. 执行**多层变更影响分析**（见下），判定受影响的 BL 条目并记录命中依据。
6. **只重新验证受影响的 BL 条目**，不要每次无脑重写全部文档；未受影响的条目保持原样（不改写内容、不重排编号、不改已有 ID）。
7. 按「时效状态判定」刷新每个条目的时效状态（四值）。
8. 按重新验证后的 BL 条目刷新 `00-源码索引.md` 的六张反向索引表，保证两边一致；新增或废弃条目时同步更新「编号登记」。
9. 按**工作区状态**决定是否推进基线（见「工作区状态与验证基线」）：
   - **Clean**：全部受影响条目重新验证完成后，才把知识地图的 `last_verified_commit` 更新为当前 HEAD（`正式基线` 同值），`工作区状态` 记 `Clean`、`验证状态` 记 `正式基线`，本次重新验证过的条目标记 `最新`。
   - **Dirty**：**不推进 `last_verified_commit`**（保持正式基线原值）、**不标记 `最新`**；`工作区状态` 记 `Dirty`、`验证状态` 记 `临时工作区分析`，本次分析的条目记 `临时工作区分析`，并在各条「最后验证版本」追加「本次分析基于未提交工作区，未推进正式基线」。
   - 两种情况都要写入「最近一次增量分析」记录。
10. 刷新传统文档新鲜度：同步模式不改 7 套传统文档，所以变更文件集非空（代码已变化）时把 `traditional_docs_status` 置 `outdated`（`traditional_docs_generated_from_commit` 与 `traditional_docs_working_tree` 保持生成时的值不变）；直到用户明确要求并重新生成 7 套传统文档（通常为重新运行生成模式），才按当时的工作区状态回到 `current`（Clean）或 `provisional`（Dirty）。
11. 同步模式**只写知识层文件**（知识地图、源码索引、BL 条目），**不改 7 套传统文档**，除非用户明确要求。

#### 工作区状态与验证基线（Clean 才推进）

判定命令：`git status --porcelain`（无输出 = Clean）。

| 工作区状态 | 允许做什么 | `last_verified_commit` | 条目标记 |
|------|------|------|------|
| **Clean**（无暂存 / 未暂存 / 未跟踪变更） | 分析、重新验证、推进基线 | 允许重新验证后推进到当前 HEAD | 重新验证过的条目记 `最新` |
| **Dirty**（有暂存 / 未暂存 / 未跟踪变更） | 可以分析、可以回答、可以更新 BL 内容 | **不得推进**（保持正式基线原值） | 记 `临时工作区分析`，**不得标记 `最新`** |

- **为什么**：BL 可能是根据**未提交的工作区代码**生成的。若用户随后用 `git restore`（或 `git checkout --`）丢弃那些未提交改动，HEAD 没有变化、工作区也重新变干净，但 BL 描述的是一份**已不存在的实现**——此时若基线已推进，系统仍会认为该条「最新」，一致性被破坏。因此工作区 Dirty 时只做临时分析，不推进正式基线。
- Dirty 时知识地图「验证基线」记录三项：`工作区状态` = `Dirty`、`验证状态` = `临时工作区分析`、`正式基线` = 正式基线 commit（未被推进）；**正式基线从未建立时 `正式基线` 与 `last_verified_commit` 同记 `未建立`** —— 这属于「有 Git 但正式基线未建立」（分支 B），**不得写成 `未发现（无 Git 基线）`**。
- **与「变更检测必须包含未提交修改」不矛盾**：变更文件集含未提交修改，用于**检测哪些条目受影响**（漏掉未提交修改会漏判过期）；Clean 才推进基线，用于**决定能否把基线推进到当前 HEAD**（Dirty 时推进会把临时分析固化成正式基线）。一个是「检测」，一个是「推进」，各管一段。
- Dirty 时 BL 条目的「最后验证版本」保留正式基线 commit，并追加一行说明「本次分析基于未提交工作区，未推进正式基线」。

#### 时效状态判定（四值）

**时效状态以 Git 变更检测为基础，并结合 Source Map 与多层影响分析判定。** 不得再声称它是 Git 单独给出的判定结果 —— 两者分工必须写清：

| 分工 | 负责什么 | 性质 |
|------|----------|------|
| **Git 负责 Change Detection（变更检测）** | `HEAD`、commit baseline、变更文件集、重命名/删除、工作区 `Clean` / `Dirty` | **纯机械结果**：命令输出即事实，不做业务解释 |
| **影响分析负责 Impact Analysis（影响分析）** | 符号影响、API 影响、数据影响、配置影响、调用链影响 | **不是纯 Git 判断**：必须结合 `00-源码索引.md`（Source Map）与各 BL 条目的多层比对得出 |
| 两者共同得到 | **Freshness Status（时效状态）** | 机械结果 + 影响分析结论，缺一不可 |

- **保留不变**：「工作区 `Clean` / `Dirty`」「commit baseline」「变更文件集」仍明确属于 **Git 机械结果**（由 `git status --porcelain`、`git rev-parse HEAD`、`git diff --name-status -M` 直接产出），不受上述表述调整影响。

| 时效状态 | 含义 |
|---|---|
| `最新` | 工作区 Clean，且证据相关文件在该条「最后验证版本」commit 与当前工作区之间无变化 |
| `⚠ 可能过期` | 上述范围内存在变化，且该条本次未重新验证 |
| `临时工作区分析` | 本次分析基于 Dirty 工作区（未推进正式基线；含「正式基线未建立且工作区 Dirty」的情况） |
| `未发现（无 Git 基线）` | 无 Git 或无有效 HEAD（**仅限分支 A**，不得用于「有 Git 但正式基线未建立」的分支 B） |

- 判定范围：某 BL「源码证据」中列出的文件，在该条「最后验证版本」commit 与**当前工作区**之间的修改（**含已提交与未提交变更**，即变更文件集中的对应部分）。
- 时效状态**以 Git 变更检测为基础，并结合 Source Map 与多层影响分析判定**，不得由模型主观填写，也不得与证据状态合并显示。

#### 变更检测（Change Detection，必须包含未提交修改）
**变更文件集 = 提交部分 ∪ 工作区部分**：

| 部分 | 命令 | 覆盖内容 |
|------|------|----------|
| 提交部分 | `git diff --name-status -M <base>..HEAD` | 基线 commit 到当前 HEAD 的已提交变更 |
| 工作区部分 | `git status --porcelain` | 已暂存、未暂存、未跟踪（`??`）变更 |

- **不能只看 `<base>..HEAD`**：代码可能已经修改但尚未提交，只比对 commit 会漏判过期，让知识层看起来是「最新」而实际与工作区代码不符。
- 用 `-M` 检测重命名：证据路径被重命名的 BL 要修正路径。
- 未跟踪文件只在分析范围内才计入（跳过 `.gitignore` 忽略项与默认跳过目录，见「分析边界与安全规则」）。

#### 变更影响分析（Impact Analysis，多层判定）
对每个变更文件逐层比对，**命中任意一层即判受影响**，并记录命中依据（命中了哪一层、哪个路径/符号/表/入口/配置项/测试）：

| 层 | 比对内容 | 命中后的处理 |
|----|----------|--------------|
| a. 路径层 | 变更文件 ∩ BL「源码证据」路径 | 该 BL 受影响 |
| b. 符号层 | diff 中修改/新增/删除的符号 ∩ BL「核心执行流程」调用链中的符号，以及「源码证据」的符号列 | 该 BL 受影响 |
| c. 数据层 | 变更涉及的表名/字段名/迁移脚本 ∩ BL「数据变化」 | 该 BL 受影响 |
| d. 接口层 | 变更涉及的路由/接口/入口签名 ∩ BL「触发入口」 | 该 BL 受影响 |
| e. 配置层 | 变更涉及的配置项/环境变量 ∩ BL「外部影响」「前置条件」 | 该 BL 受影响 |
| f. 测试层 | 变更涉及的测试文件 ∩ BL「相关测试」 | 标记「测试证据待复核」（业务逻辑本身未必失效） |
| g. 重命名/删除 | 证据文件被重命名 → 「路径失效需修复」；证据文件被删除 → 「证据文件已删除，需重新验证或废弃该条目」 | 按标记处理 |

**额外报告**：若变更文件是某 BL 调用链中符号的定义文件，但该文件不在该 BL「源码证据」里 → 标注「证据清单缺失，需补全」。

#### 知识地图「验证基线」的记录项

| 字段 | 内容 |
|------|------|
| `last_verified_commit` | 最近一次完成**正式**验证的基线 commit（工作区 Dirty 时不推进，保持原值；**从未建立时记 `未建立`**） |
| 验证日期 | 该次验证的日期 |
| `工作区状态` | `Clean` / `Dirty`（由 `git status --porcelain` 判定，**Git 机械结果**） |
| `验证状态` | `正式基线` / `临时工作区分析` |
| `正式基线` | 正式基线 commit（与 `last_verified_commit` 同值；**从未建立时记 `未建立`** —— 这是「有 Git 但正式基线未建立」，**不是** `未发现（无 Git 基线）`） |
| `traditional_docs_status` | `current` / `outdated` / `provisional`：7 套传统文档的新鲜度（三值定义见下） |
| `traditional_docs_generated_from_commit` | 7 套传统文档生成时的 commit |
| `traditional_docs_working_tree` | 7 套传统文档生成时的工作区状态：`Clean` / `Dirty` / `未发现（无 Git）` |
| 最近一次增量分析 | 基线 commit、分析日期、变更文件数（含未提交）、工作区状态、受影响 BL 列表、已重新验证的 BL、仍标记过期的 BL |

- 以上字段均由 `git status --porcelain`、`git rev-parse HEAD`、`git diff --name-status -M` 等命令产出，属 **Git 机械结果**；是否把某条目判为过期，则由 Change Detection 结果叠加 Impact Analysis 得出（见「时效状态判定（四值）」）。
- 「编号登记（ID Registry）」是紧随「验证基线」之后的独立章节，格式与取号规则见第 6 节；每次新增、删除、废弃 BL 条目都必须同步更新。

#### 传统文档新鲜度（三值 + 工作区字段，记录在知识地图）

- `traditional_docs_status` / `traditional_docs_generated_from_commit` / `traditional_docs_working_tree` **统一记录在 `00-项目知识地图.md` 的「验证基线」里**：**`00-项目知识地图.md` = 文档状态中心**。
- **01–07 仍是生成产物**，**不在 01–07 模板里增加任何 stale / current 独立字段**；判断传统文档是否最新，应读取知识地图的这三个字段，而不是去翻 01–07 文档本身。
- 同步模式**不去修改** 01–07（以保持同步模式轻量），所以传统文档的重新生成只能由用户明确要求触发。

| `traditional_docs_status` | 定义 |
|---|---|
| `current` | 传统文档由 **Clean** 工作区生成，且当前知识层未发现比其更新的代码变化 |
| `outdated` | 代码/知识层已经发生变化，但传统 7 套文档尚未重新生成 |
| `provisional` | 传统文档是在 **Dirty** 工作区下生成，内容可能包含未提交代码，**不能把 HEAD commit 当作唯一来源** |

- **生成模式**：Clean → `traditional_docs_status` = `current` + `traditional_docs_generated_from_commit` = HEAD + `traditional_docs_working_tree` = `Clean`；Dirty → `traditional_docs_status` = `provisional` + `traditional_docs_generated_from_commit` = HEAD + `traditional_docs_working_tree` = `Dirty`。
- **同步模式**（代码变化但不重新生成 01–07）→ `traditional_docs_status` = `outdated`。
- **重新运行生成模式**：Clean → `current`；Dirty → `provisional`。
- `traditional_docs_working_tree` 取值：`Clean` / `Dirty` / `未发现（无 Git）`。

#### 降级与禁止事项
- **分支 A**：无 Git 或无有效 `HEAD` 时不做时效判定，时效状态记 `未发现（无 Git 基线）`，并说明每次运行需全量复核；**不得编造 commit 哈希**。
- **分支 B**：有 Git 但正式基线尚未建立（`last_verified_commit` 为空 / `正式基线` = `未建立`）时，**禁止执行任何 `<base>..HEAD` 形式的增量 diff**（没有合法的 `<base>`），必须**全量复核**；且**禁止**把这种状态记成 `未发现（无 Git 基线）`（Git 实际存在，两者必须区分）；工作区 Dirty 时不得建立正式基线、不得标记 `最新`。
- 基线提交不可达（浅克隆、rebase、force-push）时降级为全量复核并说明原因，不得编造。
- 时效状态**以 Git 变更检测为基础，并结合 Source Map 与多层影响分析判定**，不得由模型主观填写或与证据状态合并显示；「工作区 `Clean` / `Dirty`」「commit baseline」「变更文件集」是 **Git 机械结果**，不得用影响分析结论去改写它们。
- 工作区 `Dirty` 时不得推进 `last_verified_commit`、不得出现 `最新` 标记；工作区状态、验证状态与条目时效状态必须互相一致。
- 传统文档新鲜度三字段（`traditional_docs_status` / `traditional_docs_generated_from_commit` / `traditional_docs_working_tree`）只记录在 `00-项目知识地图.md`，**禁止**在 01–07 文档里新增独立 stale / current 字段，**禁止**在同步模式里顺手改写 01–07。

### 11. 生成后校验

完成生成后必须检查：

1. 只生成用户选择的语言和文档，文件名与第 7 节完全一致。
2. 每份文档均包含项目名称、作者、日期、版本和变更日志；作者未知时明确留空。
3. 文档引用的源文件、接口、表和配置项确实存在；无法确认的条目标记为“推断”或“未发现”。
4. 数据库、API、测试和部署章节没有用模板占位内容或虚构事实填充。
5. 输出目录之外没有文件被修改，文档中没有敏感值。
6. 最终汇报生成文件、版本号、覆盖情况、分析范围、未决问题和“未发现/推断”条目。
7. 知识地图中链接的每个 `business/BL-*.md` 文件确实存在。
8. 每个 BL 条目 15 个固定章节齐全，标题与顺序与第 6 节一致，且包含「时效状态」。
9. `源码证据` 中列出的文件、符号、表真实存在。
10. 没有对应测试时写明“未发现对应自动化测试。”，不留空、不编造。
11. 每个 BL 条目都填了证据状态、时效状态与最后验证版本。
12. 时效状态的判定依据可复现（说明基线 commit、使用的命令、是否包含未提交变更，以及工作区状态 `Clean` / `Dirty`）。
13. 知识地图索引表的证据状态与时效状态**分两列独立显示**，不存在把时效标记拼接到证据状态里的合并写法。
14. `00-源码索引.md` 的**六张**反向索引表与各 BL 条目一致（路径、符号、表名、入口、配置项、外部依赖/资源、测试无缺漏、无臆造）；不一致视为校验失败。
15. BL 编号校验（四项）：① 编号无重复；② 已有 ID 不得改变；③ 历史已分配 ID 不得复用（与知识地图「编号登记」一致，含已废弃 tombstone 编号）；④ **允许因删除产生跳号**（跳号不是错误）。取号符合第 6 节「新条目编号 = 历史最大编号 + 1」。
16. 传统文档新鲜度三字段（`traditional_docs_status` / `traditional_docs_generated_from_commit` / `traditional_docs_working_tree`）齐全且记录在知识地图「验证基线」中，取值符合三值定义（`current` / `outdated` / `provisional` 与 `Clean` / `Dirty` / `未发现（无 Git）`）；01–07 文档未新增任何独立 stale / current 字段。
17. 正式基线状态校验：`last_verified_commit` 为空 / `正式基线` = `未建立` 时**未执行**任何 `<base>..HEAD` 增量 diff、已全量复核，且**未**被误记为 `未发现（无 Git 基线）`；该状态下工作区 Dirty 时未建立正式基线、未标记 `最新`。

### 12. 知识层与传统文档的关系

| 层 | 回答的问题 |
|----|------------|
| `00-项目知识地图.md`、`business/BL-*.md` 与 `00-源码索引.md` | 系统是怎么工作的？ |
| 传统 7 套文档 | 项目按照软件工程标准应该如何描述？ |

- 传统 7 套文档以知识层为事实来源。
- **`00-项目知识地图.md` = 文档状态中心**：传统文档是否最新，看知识地图「验证基线」里的 `traditional_docs_status` / `traditional_docs_generated_from_commit` / `traditional_docs_working_tree` 三个字段；01–07 是生成产物，不携带自己的 stale 标记。
- **不得在 7 份文档和 BL 条目中制造互相冲突的业务事实**；业务逻辑以最新源码证据为准。
- 若发现传统文档与已验证的 BL 条目冲突，以 BL 条目为准并修正传统文档，同时在该文档的变更日志中记录修正内容。

### 13. 真实项目验收建议

本节只定义**验收标准**，不引入任何框架、脚本或新机制。用一个真实的中大型项目完整跑一遍，逐项确认：

#### 测试 A：BL 质量
生成知识层后检查：
- BL 是否以**业务能力**命名，而不是 Controller 方法名或技术动作名？
- CRUD 是否**没有**被机械生成成大量 BL（一个资源一个 BL、一个接口一个 BL）？
- 同一业务在中英文下 ID 是否一致（`BL-00X` 两边相同）？
- 「编号登记（ID Registry）」是否正确（当前最大编号、下一个可用编号、已废弃 tombstone）？
- 是否存在明显重复的 BL（同一业务能力被拆成多条）？
- 是否遗漏核心业务能力（对照路由表/入口清单核对）？

#### 测试 B：业务查询
随机选择真实问题直接提问，例如「为什么用户不能删除这个资源？」「这个状态为什么会变化？」「创建订单后最终修改哪些数据？」「这个接口最后调用了哪个外部服务？」。验证能否沿

`问题 → Knowledge Map → BL → Source Index → 实际源码 → 业务规则 / 调用链 / 数据变化`

逐跳定位，并给出**真实代码位置**（文件 + 符号），而不是只复述文档结论。

#### 测试 C：增量失效
修改某个 Service / Domain / Route / Config / Migration（**先不更新知识文档**），验证 `git diff → 影响分析 → 正确的 BL 被标记 stale`；同时验证**无关 BL 不被错误标记**（既不漏判，也不误伤）。

#### 测试 D：Dirty 工作区
修改源码但不 commit，验证工作区 = `Dirty` 时必须满足：不得推进 `last_verified_commit`、不得标记最新、相关 BL 记 `临时工作区分析`。随后执行 `git restore` 丢弃这些未提交改动，确认系统不会把此前的临时分析结果错误地当成正式最新知识。

#### 测试 E：ID tombstone
已有 `BL-001` / `BL-002` / `BL-003`，删除 `BL-003` 后新建业务必须是 `BL-004`，**不得再次使用 `BL-003`**。

#### 测试 F：查询 stale 条目
当某条 BL 标记 `⚠ 可能过期` 时直接提问该业务，查询模式必须执行

`读取 BL → 读取 Source Index → 读取真实源码 / diff / test → 核实 → 回答`

**不能要求用户必须先执行同步模式。**

## 文档模板参考

详细的文档模板结构请参阅：
- 中文文档模板：[references/cn/document-templates.md](references/cn/document-templates.md)
- 中文业务逻辑模板：[references/cn/business-logic-template.md](references/cn/business-logic-template.md)
- 英文文档模板：[references/en/document-templates.md](references/en/document-templates.md)
- 英文业务逻辑模板：[references/en/business-logic-template.md](references/en/business-logic-template.md)

## 执行检查清单

- [ ] 项目代码已理解
- [ ] 业务知识已提取：业务能力、触发入口、业务规则、调用链、数据变化、异常路径
- [ ] 版本信息已获取（Git提交次数或时间戳）
- [ ] Doc/<项目名称>/cn/ 目录已创建
- [ ] Doc/<项目名称>/en/ 目录已创建
- [ ] `00-项目知识地图.md` / `00-Project Knowledge Map.md` 已生成，且以业务能力索引为主体
- [ ] `00-源码索引.md` / `00-Source Index.md` 已生成，六张反向索引表与 BL 条目一致
- [ ] 知识地图含「验证基线」（`last_verified_commit`、验证日期、`工作区状态`、`验证状态`、`正式基线`、`traditional_docs_status`、`traditional_docs_generated_from_commit`、`traditional_docs_working_tree`）与紧随其后的「编号登记（ID Registry）」
- [ ] BL 粒度按第 2 节「BL 粒度判定」执行（以业务能力/用例为单位，CRUD 不默认一接口一个 BL，工具函数不独立成条）
- [ ] BL 条目已生成且编号无重复、已有 ID 未改变、历史已分配 ID（含 tombstone）未被复用，新条目取号 = 历史最大编号 + 1（编号与业务域无关）
- [ ] 每个 BL 条目包含 15 个固定章节（含「时效状态」）
- [ ] 每个 BL 条目已填业务域、证据状态、时效状态与最后验证版本
- [ ] 证据状态与时效状态始终分两列独立显示，无合并写法
- [ ] 已按用户选择生成中文文档（未指定时为全部 7 份）
- [ ] 已按用户选择生成英文文档（未指定时为全部 7 份）
- [ ] 文件名、编号和目录与第 7 节完全一致
- [ ] 已处理现有文档：保留历史或获得覆盖确认
- [ ] 所有已生成文档包含作者、日期、版本号和变更日志
- [ ] 已跳过敏感文件、依赖目录和构建产物
- [ ] 已标记未发现、不适用和推断内容
- [ ] 已完成来源与证据校验
- [ ] 查询模式未写入任何文件（允许读取源码做只读核实）；同步模式只写知识层文件、未改 7 套传统文档
- [ ] 增量运行已覆盖未提交修改（`git status --porcelain`），完成受影响条目的重新验证，过期条目已标记 `⚠ 可能过期`
- [ ] 已先完成「运行前置判定」：分支 A（无 Git/HEAD 无效）走无 Git 降级；分支 B（有 Git 但正式基线未建立）未执行 `<base>..HEAD` 增量 diff、已全量复核，且未误写为 `未发现（无 Git 基线）`；分支 C 才做正常增量同步
- [ ] 工作区 Dirty 时未推进 `last_verified_commit`、未标记 `最新`，相关条目记 `临时工作区分析`；工作区 Clean 时才推进正式基线
- [ ] 传统文档新鲜度三字段记录在知识地图「验证基线」中（`current` / `outdated` / `provisional` 与 `Clean` / `Dirty` / `未发现（无 Git）`），01–07 未新增独立 stale 字段
- [ ] 同步后已按需把 `traditional_docs_status` 置 `outdated`（重新生成 7 套传统文档后才按工作区状态回到 `current` 或 `provisional`）
- [ ] 已确认没有修改输出目录之外的文件
